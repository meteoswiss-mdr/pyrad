# ---------------------------------
# This is the config file for the processing of the Emmen vol data acquired
# during the MALS_EMM_WEATHER scan performed real time in cirrus
# ---------------------------------

# Name of this configuration:
# Will be used in product generation save path
name STRING mals_emm_vol

# ---------------------------------------------------------
#  Raw data pathes
# ---------------------------------------------------------

# Base path of the raw data.
# Note: Must have a trailing '/'
# ('<datapath>/<scanname>/<YYYY-MM-DD>/<YYYYMMDDHHMMSS00datatype>.<ext>')
datapath STRING /usr/local/Rainbow5/rainbow/rawdata/144CHX_R/

# Base path for cosmo data
cosmopath STRING /usr/data/cosmo/

# Base path for dem data
dempath STRING /usr/data/DEM/DX50_EMM/

# Loading of saved datasets
loadbasepath STRING /usr/data/pyrad_products/
loadname STRING mals_emm_vol
    
# --------------------------------------------
# Output data
# --------------------------------------------
saveimgbasepath STRING /usr/data/pyrad_products/
saveimg         INT 1
imgformat       STRING png

# --------------------------------------------------------
# configuration files pathes
# --------------------------------------------------------

# Base path of configuration files
configpath STRING $HOME/pyrad/config/

# Configuration of the radar location and approach direction
locationConfigFile STRING $HOME/pyrad/config/processing/mals_emm_vol_loc.txt

# Configuration of the product generation
productConfigFile STRING $HOME/pyrad/config/processing/mals_emm_vol_prod.txt

# State after last run
lastStateFile STRING $HOME/realtime_pyrad/mals_emm_vol_rt_lastState.txt

