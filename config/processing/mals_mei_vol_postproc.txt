# ---------------------------------
# This is the config file for the processing of the Meiringen vol data acquired
# during the MALS_MEI_WEATHER scan performed off-line the next day in zueub222
# ---------------------------------

# Name of this configuration:
# Will be used in product generation save path
name STRING mals_mei_vol

# ---------------------------------------------------------
#  Raw data pathes
# ---------------------------------------------------------

# Base path of the raw data.
# Note: Must have a trailing '/'
# ('<datapath>/<scanname>/<YYYY-MM-DD>/<YYYYMMDDHHMMSS00datatype>.<ext>')
datapath STRING /data/DX50/rawdata/

# Base path for cosmo data
cosmopath STRING /data/cosmo/

# Base path for dem data
dempath STRING /data/DEM/DX50_MEI/

# Loading of saved datasets
loadbasepath STRING /data/pyrad_products/
loadname STRING mals_mei_vol
    
# --------------------------------------------
# Output data
# --------------------------------------------
saveimgbasepath STRING /data/pyrad_products/
saveimg         INT 1
imgformat       STRING png

# --------------------------------------------------------
# configuration files pathes
# --------------------------------------------------------

# Base path of configuration files
configpath STRING $HOME/pyrad/config/

# Configuration of the radar location and approach direction
locationConfigFile STRING $HOME/pyrad/config/processing/mals_mei_vol_loc.txt

# Configuration of the product generation
productConfigFile STRING $HOME/pyrad/config/processing/mals_mei_vol_prod.txt

# State after last run
lastStateFile STRING $HOME/postproc_pyrad/mals_mei_vol_postproc_lastState.txt

